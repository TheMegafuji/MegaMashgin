# Initial source brief

This records the initial scope. Later requests added optional Jev discovery, the Oracle deployment and a separate game client; see the [build log](build-log.md) and [current documentation](../README.md). The original exclusions below describe that starting point.

The user requested a new implementation in the parent Mashgin directory, based on the accumulated study, with the product implemented from scratch. They explicitly removed the on-page game and asked for current AI development practices and a visible, structured process in the folder.

Interpretation accepted for this build:

- Deliver the actual customer-facing checkout.
- Keep React, Fastify and PostgreSQL from the justified architecture choices.
- Preserve historical studies locally, outside the new product's tracked/build inputs.
- Expose AI-assisted construction through specs, decisions, source history and verification.
- Do not add paid runtime AI or copy credentials.
- Prepare and run locally; do not silently replace the earlier public prototype.

This is a concise record of the brief, not a complete or fabricated conversation transcript.
